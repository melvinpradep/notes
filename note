<?xml version="1.0" encoding="UTF-8"?>
<jmeterTestPlan version="1.2" properties="5.0" jmeter="5.6.2">
  <hashTree>
    <ThreadGroup testname="Performance Test">
      {% if Perf.DurationSeconds %}
      <stringProp name="ThreadGroup.duration">{{ Perf.DurationSeconds }}</stringProp>
      {% endif %}
    </ThreadGroup>

    {% if API.SendRequest %}
    <HTTPSamplerProxy testname="{{ API.Method }} {{ API.Endpoint }}">
      <stringProp name="HTTPSampler.domain">{{ API.Host }}</stringProp>
      <stringProp name="HTTPSampler.path">{{ API.Endpoint }}</stringProp>
      <stringProp name="HTTPSampler.method">{{ API.Method }}</stringProp>
    </HTTPSamplerProxy>
    {% endif %}

    {% for a in Assertions %}
    <JSONAssertion testname="Assert {{ a.JSONPath }}={{ a.Equals }}"/>
    {% endfor %}
  </hashTree>
</jmeterTestPlan>
